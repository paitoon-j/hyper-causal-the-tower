public enum EState
{
    LOGIN,
    HOME,
    MAIN,
}

public enum EGlobalEvent
{
    // popup pause
    BACK_TO_HOME,
    BGM,
    SFX,

    // popup selection
    START_GAME,
    ON_HEAD_START,
    ON_SLOW_TIME,

    // popup reward
    ADS_REWARD,
    BACK_TO_MENU,
}
